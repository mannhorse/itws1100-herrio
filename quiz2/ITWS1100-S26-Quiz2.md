# ITWS 1100 — Quiz 2 (Spring 2026)

**Name:** ________________________________________________________________________

## Directions

There are 4 sections of multiple parts. Point values and suggested times are indicated next to each question.

- Place your name on the top of the document in the header
- Enter your answers directly into a copy of this document named: **ITWS1100-S26-Quiz2-yourRCSid.docx**
- Create a branch in your repo called `quiz2` and switch to that branch
- Create a folder in your repo under the root of your website for this quiz called `quiz2`
- Save this document, and all quiz-related files (HTML, CSS, JS) into that folder
- Create a `readme.md` in the same folder with your GitHub ID, repo name, Azure homepage link, and Discord handle
- All written answers must be in complete, well-formed paragraphs with proper grammar and punctuation. One-sentence or bulleted-list-only responses will receive no credit.
- Write in Your Own Words — reference content and examples from class throughout
- Make sure your answers use an alternative font AND color — (a legible font, and not Black or Red)
- Make sure your submitted document remains in MS Word format (Pages, GDocs, etc. will NOT be graded.)
- Commit your changes and push to GitHub. **DO NOT** create a pull request or merge into main.
- When finished, also submit a zip of your quiz2 folder to LMS
- Answers that appear to be generated by AI tools without demonstrating personal understanding and class-specific examples will result in a failing grade on this quiz.

**NOTE:** You are not to discuss this quiz with anyone. You are not to reference old (previous semester) submissions for 'help' or guidance. You may not solicit or receive help online or in-person. You may reference online resources and the notes from this class, but all work must be your own and you must figure out the solutions on your own. If you use AI for assistance, you must state explicitly what you asked and what was returned in your readme file.

---

## 1. Technology — Coding (35 points, ~30 minutes)

Given the `quiz2.html`, `quiz2.css`, and `quiz2.js` starter files, make the following modifications:

**In the HTML:**

a. Change whatever is necessary to ensure the file adheres to **HTML5** syntax. (There are intentional issues with the current XHTML doctype, meta tags, and self-closing syntax.)

b. Connect your HTML file to **jQuery** via CDN (use the latest 3.x version).

c. Add your full name in an `<h4>` tag below the existing `<h1>` tag.

d. Add additional tagging (your choice — span, id, class, etc.) that can be used to identify and target your **last name** specifically.

e. Add a `<button>` at the bottom of the page with an id of your choosing, labeled **"Go"** on the page.

**In the JavaScript file (quiz2.js):**

f. Show an `alert()` that the page is about to load (this should fire *before* the DOM is ready).

g. Add a function that executes when the document is finished loading (**HINT:** You will need the jQuery `ready` function). Inside this function:

- Set the `<title>` of the page to **"ITWS 1100 - Quiz 2"** to act as the default.

h. Using JavaScript with or without jQuery:

- When **"Go"** is clicked and the title is the default → change the title to **"YourName – Quiz 2"**
- When **"Go"** is clicked and the title is NOT the default → change the title back to the default
- When the user **hovers** the mouse over your last name → add a class of `makeItPurple` to your last name
- When the user **stops hovering** → remove the `makeItPurple` class

**In the CSS file (quiz2.css):**

i. Add the CSS rule for `.makeItPurple` that makes the text **purple**, **italic**, and in **small-caps**.

j. Make the content of both the `<h1>` and `<h4>` tags **centered**.

**Your completed webpage should look similar to this behavior:**

- Page loads → alert fires → title shows "ITWS 1100 - Quiz 2"
- Click "Go" → title changes to your name
- Click "Go" again → title reverts to default
- Hover over last name → text turns purple, italic, small-caps
- Stop hovering → text reverts to normal

---

## 2. Technology — Description (25 points, ~20 minutes)

**a. (10 points, ~8 min)** In Lab 5 you wrote vanilla JavaScript. In Lab 6 you used jQuery to accomplish similar tasks. Compare the two approaches:

- How does selecting an element differ? Show both vanilla JS and jQuery syntax with specific examples from your labs.
- How does hiding/showing an element differ in both approaches?
- What is the `$(document).ready()` pattern and why does it matter? When would your code fail without it?
- What does the `$` do in jQuery? What would you do if another JavaScript library was already using `$`?

Reference your actual Lab 5 and Lab 6 code — generic descriptions will not receive full credit.

**b. (10 points, ~8 min)** Explain, in explicit detail, what is happening in each of the following code samples. Describe what each line does, what the user would see, and any potential issues:

i.

```javascript
$(document).ready(function() {
  let score = 85;
  let passing = 70;
  if (score >= passing) {
    $("#result").text("You passed!").css("color", "green");
  } else {
    $("#result").text("Try again.").css("color", "red");
  }
});
```

ii.

```css
.highlight {
  font-family: "Courier New", monospace;
  font-weight: 700;
  font-size: 120%;
  background-color: #ffffcc;
  padding: 5px;
}
```

iii.

```javascript
$(".card").on("click", function() {
  $(this).toggleClass("highlight");
});
```

iv.

```css
.sidebar { height: 100%; width: 250px; float: left; clear: left; }
.content { margin-left: 260px; }
```

**c. (5 points, ~4 min)** Your classmate says their JavaScript code runs but "nothing happens on the page." The console shows no errors. Walk through your debugging process: what Developer Tools features would you use, what would you check first, second, third? Describe at least three specific debugging steps and what each one tells you. Reference a specific debugging experience from your own labs.

---

## 3. HCI & Case Studies (20 points, ~15 minutes)

**a. (9 points, ~7 min)** Recalling the HCI discussion in class: what are two main principles of interface design? How are they applied in practice? Relate these principles to the Lab 7 mockups assignment — how did you (or should you) apply these principles when designing your team's project mockups?

**b. (11 points, ~8 min)** In the Kuehchic Desserts case, you analyzed whether the team could launch by September 1, 2022. Now apply what you learned to YOUR team's term project:

- Define "critical path" and "float" in your own words. Identify what you believe is the critical path in your team's project — what sequence of tasks, if any one is delayed, would push back your entire project delivery?
- The Kuehchic team had a triple constraint problem (scope, time, cost). Which of these three constraints is the tightest for YOUR team's project right now, and what is your team doing (or should be doing) to manage it?
- Based on the Kuehchic analysis, what is one mistake you see the Kuehchic team making that your team should avoid? Be specific — reference a task or decision from the case.

---

## 4. Development Workflow & AI Reflection (20 points, ~15 minutes)

**a. (8 points, ~6 min)** Throughout this course you have been working with a development workflow that involves your **local machine**, **GitHub**, and your **Azure VM**. Answer the following:

- Describe the role each of these three environments plays in your workflow. What do you do on each one, and how do changes move between them?
- You are working on your `quiz2` branch locally and have made changes to several files but have **not committed** them. You then run `git checkout main`. What happens to your uncommitted changes? What would you see in your file explorer — would the quiz2 folder and files still be there? Explain why or why not.
- Your classmate says "I pushed my code to GitHub but my website on Azure still shows the old version." Explain what step(s) they likely forgot and why pushing to GitHub does not automatically update the Azure VM.
- Why is it important to work on a **separate branch** (like `quiz2`) instead of committing directly to `main`? What problems could arise if the entire class committed directly to `main` on a shared project?

**b. (5 points, ~4 min)** Given your experience deploying to your Azure instance: if the root of your web server were to change from `/var/www/html` to `/webserver/www`, what specific changes would you need to make to your deployment process? Think about: Apache configuration, your Git remote/pull workflow, file permissions, and any references in your code that use absolute paths. Be specific to YOUR setup.

**c. (7 points, ~5 min)** You have now completed multiple labs both with and without AI assistance, and you used the CLEAR method in Quiz 1. Based on your experience since then:

- Describe a specific situation from Labs 5 or 6 where AI helped you understand a concept (like event handling or jQuery selectors) that you were struggling with. What did you ask, what did it return, and how did you verify it was correct?
- Describe a specific situation where you chose NOT to use AI and worked through the problem yourself. Why did you make that choice, and what did you learn from the struggle?
- Has your personal rule for when to use AI vs. working independently changed since Quiz 1? If so, how and why?
